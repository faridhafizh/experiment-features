## 2026-05-16 - Improved PJP Dashboard Empty State
**Learning:** Replaced a simple text empty state with an engaging UI block including an icon and call-to-action button, successfully improving the visual guidance for first-time users without adding any new CSS classes.
**Action:** When adding empty states, always consider adding an actionable button and a visual icon using existing utilities to guide users.
## 2026-07-07 - Proper Form Labeling and Feedback\n**Learning:** Implementing explicit uid=1001(jules) gid=1001(jules) groups=1001(jules),27(sudo),103(docker) to  bindings in forms, adding visual required indicators, and disabling submit buttons on form submission significantly improves screen reader accessibility and prevents duplicate form submissions. This is a crucial pattern for EJS-based server-side rendered apps.\n**Action:** Always verify /uid=1001(jules) gid=1001(jules) groups=1001(jules),27(sudo),103(docker) bindings, ensure required fields are clearly marked visually and programmatically, and provide visual feedback for pending form submissions.
## 2026-07-07 - Form Accessibility and Submit Feedback
**Learning:** Implementing explicit id to for bindings in forms, adding visual required indicators, and disabling submit buttons on form submission significantly improves screen reader accessibility and prevents duplicate form submissions. This is a crucial pattern for EJS-based server-side rendered apps.
**Action:** Always verify for/id bindings, ensure required fields are clearly marked visually and programmatically, and provide visual feedback for pending form submissions.

## $(date +%Y-%m-%d) - Form Accessibility and Submit Feedback
**Learning:** Implementing explicit `id` to `for` bindings in forms, adding visual required indicators, and disabling submit buttons on form submission significantly improves screen reader accessibility and prevents duplicate form submissions. This is a crucial pattern for EJS-based server-side rendered apps.
**Action:** Always verify `for`/`id` bindings, ensure required fields are clearly marked visually and programmatically, and provide visual feedback for pending form submissions.
